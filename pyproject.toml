[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "quilt-papers"
version = "0.1.0"
description = "Canon to academic papers"
readme = "README.md"
requires-python = ">=3.8"

[tool.setuptools]
packages = ["quilt_papers"]
